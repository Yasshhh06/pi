{
  "name": "valentines-day-letter",
  "version": "1.0.0",
  "description": "Interactive Valentine's Day Letter",
  "main": "index.html",
  "scripts": {
    "dev": "npx vite --port 8080 --host",
    "build": "npx vite build",
    "preview": "npx vite preview"
  },
  "dependencies": {
    "vite": "^6.0.0"
  }
}
